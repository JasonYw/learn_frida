syntax = "proto3";

message ProtoAdapter_EditAwemeBarInfo{
    int32 valueOf_buttonStatus = 1;
    string button_toast = 2;
    string bar_toast = 3;
    // ProtoAdapter.STRING.encodeWithTag(protoWriter, 2, editAwemeBarInfo.buttonToast);
    // ProtoAdapter.STRING.encodeWithTag(protoWriter, 3, editAwemeBarInfo.barToast);
}